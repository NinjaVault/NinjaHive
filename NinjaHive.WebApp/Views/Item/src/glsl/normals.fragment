precision mediump 	float;

varying vec2 		outUvs;
varying vec3 		outNormals;
varying vec3 		outPositions;

void main(void) 
{
	vec3 outColor = outNormals.xyz / 2.0 + 0.5;
	gl_FragColor = vec4(outColor, 1.0);
}
